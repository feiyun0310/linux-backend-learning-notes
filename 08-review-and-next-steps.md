# 08 复盘与下一步

## 已建立的能力

这批资料已经覆盖了 Linux 后端开发的一条核心主线：

- 创建并管理线程。
- 用锁、条件变量和信号量保护共享状态。
- 设计有界阻塞队列和生产者消费者模型。
- 理解同步日志与异步日志的取舍。
- 编写 TCP 客户端和服务端。
- 使用 select/epoll 管理多个连接。
- 把网络、业务和数据层拆成独立组件。

## 当前最值得加强的部分

### 从示例代码到可验证代码

- 使用 Makefile 或 CMake 固化编译。
- 开启全部常用警告并处理警告。
- 使用 AddressSanitizer、UndefinedBehaviorSanitizer。
- 使用 ThreadSanitizer 检查数据竞争。
- 对阻塞队列做高并发、超时和关闭测试。

### 完善 TCP 协议

重点包括输入/输出缓冲、长度前缀、最大帧限制、请求 ID、超时和错误码，以及半包、粘包和部分写测试。

### 完善事件循环

把连接状态封装为对象：

- 输入缓冲。
- 输出缓冲。
- 登录状态。
- 最后活跃时间。
- 会话代次。

网络线程只操作连接对象；工作线程通过线程安全队列返回响应，避免多个线程直接并发修改 epoll 状态。

### 完善服务拆分

在 Client、LogicServer、DataServer 基础上继续学习：

- Redis 数据结构和持久化。
- Protobuf 协议演进。
- ZooKeeper 服务注册与发现。
- 多实例路由和故障转移。
- 指标、追踪和结构化日志。

## 推荐回归场景

1. 两个客户端进入同一频道并互发消息。
2. 新客户端登录后能收到历史记录。
3. 客户端发送半包、多个粘连包和超长包。
4. DataServer 先停止再恢复，LogicServer 能重连。
5. 慢客户端不影响其他连接。
6. 多线程同时写日志，退出时不丢尾部数据。
7. 服务器收到 SIGINT/SIGTERM 后能有序退出。
8. 重复请求不会生成重复聊天记录。

## 学习闭环

~~~text
写清需求
  → 画出数据流和线程模型
  → 定义协议与错误行为
  → 编写最小实现
  → 编译和静态检查
  → 正常/异常/压力测试
  → 记录问题并提交
~~~

当这些步骤成为习惯，学习重点就会从“记住接口”转向“设计可运行、可定位、可恢复的系统”。

