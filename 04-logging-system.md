# 04 日志系统

## 功能演进

日志代码体现了一条清晰的迭代路线：

1. 日志级别过滤和控制台输出。
2. 使用宏自动记录文件名、行号和函数名。
3. 增加文件输出，不同输出目标可设置不同最低级别。
4. 引入后台线程与双缓冲，减少业务线程等待磁盘 IO 的时间。
5. 增加文件滚动、备份保留和分类日志。

常见级别顺序：

~~~
TRACE < DEBUG < INFO < WARN < ERROR < FATAL
~~~

## 同步日志

同步日志实现简单，调用者直接完成格式化和输出。多线程写同一流时必须加锁，否则日志行会交叉。

优点是写入结果明确；缺点是磁盘变慢时会直接拖慢业务线程。

## 异步日志

异步日志把业务线程视为生产者，把后台刷盘线程视为消费者：

~~~mermaid
flowchart LR
    A[业务线程] -->|格式化并入队| B[写缓冲]
    B -->|交换缓冲| C[刷盘缓冲]
    C --> D[后台线程]
    D --> E[文件]
~~~

关键原则：

- 业务线程只在操作缓冲区时短暂持锁。
- 后台线程取走一批日志后立即解锁，再执行慢速写盘。
- 缓冲区必须有容量上限和明确的满载策略。
- 停止时需要唤醒后台线程并刷完剩余日志。

满载策略可以是阻塞、超时、丢弃低级别日志或同步降级。无论选择哪种，都要记录丢弃数量。

## 多输出目标与分类

输出目标可以包括控制台、普通文件、滚动文件或网络。每个输出目标可配置独立阈值，例如 default=INFO、network=WARN、database=ERROR、security=INFO。

文件滚动通常根据大小或时间触发。大小滚动流程为关闭当前文件、重命名历史文件、创建新文件并删除超出保留数量的旧文件。

## 原代码中的复查点

- 最终代码中的 LOG_CATEGORY_INFO 宏调用了 WARN，应检查是否为笔误。
- std::localtime 可能使用共享静态存储，多线程环境应使用 localtime_r 或等价线程安全接口。
- 宏应使用 do { ... } while (0) 包装，避免放入 if/else 后产生语义问题。
- 文件创建、滚动失败必须有可见的降级策略。
- flush 频率需要在可靠性和吞吐量之间权衡。
- 日志消息应携带线程 ID、分类、时间、级别和位置，但要避免在热点路径执行过重格式化。

## 测试建议

- 多线程连续写入，检查日志行是否完整。
- 人为降低写盘速度，观察业务线程延迟和丢弃数量。
- 使用极小滚动阈值验证备份顺序。
- 在队列仍有数据时退出，检查是否完成最终刷盘。
- 模拟目录不存在、磁盘满和权限不足。



## 完整代码案例

PDF 中的多版日志代码展示了从同步输出、宏封装、异步缓冲到文件滚动和分类日志的演进。由于原稿跨页后存在漏行和括号缺失，这里整理为一个完整的 C++17 示例：

- [查看完整源码：examples/logging_system.cpp](examples/logging_system.cpp)
- 支持 `TRACE` 到 `FATAL` 的级别过滤。
- 宏自动记录文件名、行号和函数名。
- 业务线程写入有界队列，后台线程批量刷盘。
- 日志文件达到大小上限后自动滚动，并保留指定数量的备份。
- 可以为 `network` 等分类单独设置最低日志级别。

```bash
g++ -std=c++17 -O2 -Wall -Wextra -pthread \
  examples/logging_system.cpp -o logging_system
./logging_system
```

运行后，控制台会显示日志，文件日志写入 `./log/app.log`；文件达到示例设置的 64 KiB 后会依次生成 `app.log.1`、`app.log.2` 和 `app.log.3`。
