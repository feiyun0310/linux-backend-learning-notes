# 01 学习路线与做事方法

## 总体目标

原始计划不只是学习若干 API，更强调形成独立完成任务的闭环：

~~~
思考 → 方案 → 编码 → 自测 → 反馈 → 产出
~~~

每项练习都应该留下可验证结果：源码、编译命令、测试记录、问题复盘，而不是只记住函数名。

## 第一阶段：Linux 与并发基础

### 工具链

- 熟悉 Linux 命令行、目录和进程。
- 使用 gcc/g++ 编译，使用 Makefile 管理多文件项目。
- 使用 gdb 定位崩溃、死锁和变量异常。
- 复习 C++ 类、STL 容器、RAII 和线程相关库。

### 并发能力递进

1. 创建线程并正确等待线程结束。
2. 观察多个线程同时修改计数器时的数据竞争。
3. 使用互斥锁保护临界区。
4. 理解读写锁适用于读多写少的共享数据。
5. 使用信号量控制资源数量或事件顺序。
6. 封装有容量限制的阻塞队列。
7. 通过银行账户、生产者消费者和日志系统完成综合练习。

## 第二阶段：网络编程

学习 socket、bind、listen、accept、connect、recv、send 和 close，先完成一对一 TCP 聊天，再进入：

- select：用 fd 集合等待多个描述符。
- epoll：注册感兴趣的事件，直接获得就绪 fd 列表。

最终练习是多人聊天室，再把客户端、逻辑服务和数据服务组合成游戏聊天服务器。

## 每个主题的学习检查

1. 它解决了什么问题？
2. 不使用它会发生什么？
3. 正常流程和异常流程分别是什么？
4. 如何写测试证明实现是正确的？

## 推荐产出习惯

- 每天提交一个可运行的最小版本。
- 每次提交只解决一个明确问题。
- README 写清依赖、编译、运行顺序和已知限制。
- 为并发代码增加压力测试，为网络代码增加断线、半包和慢客户端测试。
- 发现概念或代码错误时，保留“为什么错”和“怎么验证”的记录。

