# 06 TCP 客户端与服务端

## Socket 的角色

- IP 地址定位主机。
- 端口定位主机上的服务。
- 监听 socket 只负责接收新连接。
- accept 返回的通信 socket 负责和某个客户端交换数据。

## 服务端流程

~~~text
socket
  ↓
setsockopt（可选）
  ↓
bind
  ↓
listen
  ↓
accept
  ↓
recv/send
  ↓
shutdown/close
~~~

## 客户端流程

~~~text
socket → connect → send/recv → shutdown/close
~~~

connect 成功只表示传输通道已建立，不表示应用层登录或鉴权成功。

## recv 返回值

- 大于 0：成功读取对应字节数。
- 等于 0：对端已经有序关闭发送方向。
- 小于 0：发生错误；非阻塞 socket 还要区分 EINTR、EAGAIN 和 EWOULDBLOCK。

## TCP 是字节流

TCP 保证字节顺序，不保留应用消息边界。发送两次不代表接收方也会 recv 两次，因此协议必须定义拆包方式：

- 固定长度。
- 分隔符。
- 长度前缀。
- TLV 或 Protobuf 外加长度头。

长度前缀协议常用 4 字节网络序表示正文长度：

~~~text
[uint32 length][payload bytes]
~~~

接收端维护输入缓冲，只有累计到完整帧后才交给业务层。

## 简单聊天程序

基础练习采用两个执行流：

- 接收线程阻塞等待服务端消息。
- 主线程读取标准输入并发送。

退出时建议：

1. 设置停止状态。
2. 调用 shutdown 唤醒可能阻塞的接收。
3. 等待接收线程退出。
4. 最后 close fd。

强制取消线程可能使锁、缓冲和终端状态无法正确清理，应尽量通过关闭事件和状态变量结束线程。

## 网络代码检查表

- 地址、端口和字节序是否正确？
- 是否处理短读、短写、粘包和半包？
- 是否限制最大消息长度？
- 是否忽略 SIGPIPE 或使用 MSG_NOSIGNAL？
- 是否设置连接、读取和请求超时？
- 异常路径是否删除事件监听并关闭 fd？
- 多线程写同一个连接时是否串行化？
- 日志是否包含连接 ID 和请求 ID？

