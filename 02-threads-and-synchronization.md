# 02 线程与同步

## 线程生命周期

pthread 线程函数的固定形式是：

~~~c
void* worker(void* arg);
~~~

基本流程是声明 pthread_t、调用 pthread_create、检查返回值、最后使用 pthread_join 等待结束。

需要特别注意：

- 传给线程的指针在工作线程使用完之前必须仍然有效。
- 主线程提前退出会结束整个进程。
- 线程应该有明确的停止条件，不能依赖强制取消。
- 创建成功的线程必须被 join 或 detach。

## 数据竞争与互斥锁

当多个线程访问同一对象，并且至少一个线程会写入时，如果没有同步，就可能产生数据竞争。结果包括计数错误、读取中间状态、容器损坏和随机崩溃。

互斥锁保证同一时刻只有一个线程进入临界区。锁的范围应只覆盖共享状态操作，磁盘 IO、网络 IO、休眠和复杂计算通常不应在持锁期间执行。

## 读写锁

读写锁允许多个读线程并行，但写线程需要独占。它适合“读多写少且读操作时间不短”的场景；如果临界区很小，普通互斥锁可能更合适。

常见接口：pthread_rwlock_rdlock、pthread_rwlock_wrlock 和 pthread_rwlock_unlock。

## 条件变量

条件变量用于等待“某个状态已经成立”，必须与互斥锁配合：

~~~c
pthread_mutex_lock(&mutex);
while (!condition_is_true()) {
    pthread_cond_wait(&cond, &mutex);
}
// 使用受保护的共享状态
pthread_mutex_unlock(&mutex);
~~~

必须使用 while 重新检查条件，因为可能发生虚假唤醒，也可能有其他线程先一步改变状态。

## 信号量

信号量本质上是非负计数器：

- sem_wait：计数大于 0 时减一，否则等待。
- sem_post：计数加一，并可能唤醒等待线程。

它适合限制最大并发数、表示可用资源数量，以及实现简单的等待/通知关系。sem_post 增加的是信号量计数，不是“直接增加业务数据”。

## 死锁

死锁通常同时满足互斥、持有并等待、不可抢占、循环等待四个条件。

规避方法包括固定锁顺序、缩短持锁时间、避免持锁等待信号量或阻塞 IO，以及必要时使用 trylock、超时和失败回退。

## 并发代码检查表

- 共享变量由哪把锁保护？
- 是否存在漏解锁或重复解锁？
- 等待条件是否使用 while？
- 停止时能否唤醒所有等待线程？
- 锁的获取顺序是否一致？
- 是否在持锁期间调用不受控的外部函数？

